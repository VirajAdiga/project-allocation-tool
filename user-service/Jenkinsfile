pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                dir('user-service') {
                    bat 'echo Building the artifact'
                    bat "call mvnw.cmd clean compile test package"
                }
            }
        }

        stage('Archive Artifacts') {
            steps {
                dir('user-service'){
                    bat 'echo Archiving the artifact'
                    archiveArtifacts 'target/*.jar'
                }
            }
        }
    }
}
